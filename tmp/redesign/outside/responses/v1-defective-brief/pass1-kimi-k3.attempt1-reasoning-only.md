---
model: kimi-k3
pass: 1
endpoint: https://opencode.ai/zen/go/v1/chat/completions
date: 2026-08-20T21:20:47.252Z
prompt: PROMPT-pass1.md
prompt_sha256: eab389889e055bcb32bc4532edc2183f780d8a267227464b47a9a9e220cae542
finish_reason: null
tokens: null
wall_seconds: 510
truncated: false
reasoning_chars: 59711
---


<details><summary>Model reasoning trace (59711 chars) — kept because rejected alternatives often live here</summary>

```
Let me think hard about this design brief. The user wants a full protocol design, argued from first principles, for decentralized personal publishing with a hostile hub operator as the central adversary. Four floor assurances:

1. Host cannot speak for you (signing, keys held by user).
2. Host cannot read content not meant for it (end-to-end encryption to chosen recipients).
3. Host cannot keep you (exit at any time, identity portable, because key + copy on device).
4. Cross-hub first-class interactions (no access-control agreements between hubs).

Priorities: no dependencies (Ed25519, SHA-256, HTTP, JSON, base64 only — stdlib), easy to implement, interop with RSS/Atom/JSON Feed and fediverse/Bluesky bridges, minimal.

Key design questions:

**Identity primitive.** The obvious answer given "no global registry" and "identity survives domain loss": a person is an Ed25519 public key. But how do people *find* each other and how does a key bind to a location? Options:

- DID-style: did:key, did:web — but that needs a method spec; did:key is fine (just multibase encoding of the key) but multibase/multicodec is arguably an extra dependency. I can define my own base64url encoding.
- Key as identity, hub as hint: identity = public key; the hub URL is a "location hint" that can change, signed by the key.
- Petname/introduction-based discovery: you learn someone's key out of band (in person, via a family member's introduction signed by them).

The scenario "domain goes, everyone moves, existing readers find them" requires that readers follow the *key*, not the domain. So the canonical fetch is: given a pubkey, fetch its current hub from... where? You need at least one previously-known hub to ask "where is key K now?" If all known hubs are gone, you need a new introduction out of band. That's an honest limitation: the protocol provides key-based redirect (a signed relocation statement) cached by readers and served by any hub that knows it. Readers remember old hubs; a move is announced in advance via signed relocation documents, and after the fact any hub that has seen the relocation statement can serve it (gossip of relocation). Actually simpler: the relocation statement is itself a signed artifact; anyone can hold and serve it; readers poll old hubs and also can query any hub they know: "do you have a current location for key K?" Hubs can cache relocation statements they've seen. That's soft-state gossip, no DHT.

So identity: `pk` = base64url(Ed25519 pubkey, 32 bytes). Displayed form maybe `k:<base64url>` or a "code" for introduction: a URL like `https://hub.example/u/<pk>` which contains both location and key — the key is in the URL so the introduction is self-certifying. That's the crucial trick: the address you share is a URL containing the pubkey. Like `https://family.example/people/<pk>/`. Any reader fetching that verifies the served document is signed by `<pk>`. Domain loss doesn't change the key; the URL changes but the key persists.

**Artifacts.** Let me enumerate minimally:

1. **Identity document / self-certifying manifest**: signed by the key, contains: current hub URL(s), display name, avatar hash maybe, public content feed location, encryption public key(s), sequence number + timestamp (monotonic), previous-version hash (chain). This is the thing you fetch on first contact and poll. Call it the **card**. Actually let's think: do we need a chain (hash-linked log) for the card? The hostile host can freeze (serve old version). Monotonic counter + timestamp lets readers detect stale *if they have seen newer*. Chain of hashes gives fork detection — an equivocation log. For "his frozen copy reads as stale rather than as her", we need: after she moves, readers see the relocation signed by her key; the old hub's copy, if anyone fetches it, contains an older sequence number; but a *new* reader who only knows the old URL would fetch stale content that verifies! How do they know it's stale? The card should include the current hub pointer; the stale copy's card points at old hub. Hmm — she relocates: her new card (seq N+1) says "hub = newhub.example, previous hub = family.example". A stranger with the old URL fetches the frozen copy: card seq N, hub = family.example, content verifies. Nothing flags it as stale *to them* unless the card contains a "check also" pointer or unless staleness is detectable. Options: (a) cards carry an expiry ("valid until" / "check for newer by date"), forcing periodic re-signing; a frozen copy eventually expires and readers display "this identity has not been reaffirmed since <date>; it may be stale or abandoned." That makes frozen copies *read as stale*, satisfying the divorce scenario literally: expiry-based staleness. She re-signs periodically (app does it automatically when online). The hostile ex's frozen archive will age past expiry and readers label it stale. Cost: keys must sign periodically — fine, app does it. (b) reader-side: if you've seen a higher seq, old is stale — but new readers haven't seen anything. Expiry is the robust answer. I'll adopt "cards expire; default lifetime e.g. 90 days; apps re-sign monthly." This is a nonobvious choice; rejected alternative: perpetual cards + sequence numbers only (fails new-reader staleness).

Also cards should carry a hash-chain prev pointer? For equivocation detection by the *subject*: she can fetch her own card from the hub and detect a fork (hub serving different cards to different people). With a hash chain, fork evidence is compact: two cards with same seq and different prev/content = proof of misbehavior anyone can verify. That's cheap to include: `prev: sha256(previous card bytes)`. I'll include it. It also gives "alter or backdate what she wrote" protection for the card history.

2. **Items (posts)**: each item is a signed JSON envelope: `{v, id (hash), author pk, seq or timestamp, kind (note/article/photo/reply/reaction), in-reply-to refs (by item hash), audience marker (public or encrypted), body or ciphertext, sig}`. Content-addressed by hash: `id = sha256(canonical bytes)`. Wait — "no JSON canonicalizer" constraint! So signatures must be over *bytes*, not over canonicalized JSON. Standard trick: sign the exact byte string; transmit the signed payload as an opaque string. So an item is:

```
{
  "sig": base64url(sig),
  "payload": base64url(json bytes)
}
```

The payload bytes are the JSON; verifiers decode, check hash = sha256(payload bytes), verify sig over payload bytes (or over a domain-separated prefix + payload). No canonicalization needed because we never re-serialize. The payload JSON inside can be pretty-printed however the author likes; bytes are the truth. This is the key implementability decision and I should state it loudly. JCS (RFC 8785) is rejected because it's an extra spec/dependency; signing raw bytes is strictly simpler.

Item ids: `sha256(payload)` — actually the hash should commit to the signature too? No: id should commit to content; sig can be stripped/added. If id = sha256(payload), then anyone can re-wrap with a new sig — but only the author can produce a valid sig, so fine. Two identical posts (same payload) would collide — include a nonce or timestamp in payload; fine, note it.

3. **Feed / index**: for interop with RSS/Atom/JSON Feed, public items must be exposable as a standard feed. The hub can generate RSS from public items; but feed content served by hub isn't signed per-entry in RSS... Interop priority says content reaches existing feed readers "with nothing extra built". So the hub (or the publisher) publishes a JSON Feed / RSS rendering of public items. The feed is convenience; verification happens via the signed items available at a parallel endpoint. For a *stranger in an ordinary feed reader*, they just read; no verification (they trust the hub for read-only public content — acceptable; the floor is about the host not *speaking for you*, i.e., forgery that others accept as authentic; a stranger's RSS reader trusting the hub is a degraded mode we acknowledge). But we can do better: include per-entry signatures? Not possible in plain RSS without extensions. So: public interop feed is unsigned, marked as convenience; verifying clients fetch the signed log. Honest cost.

4. **Encrypted items**: for audience-restricted content. Construction must be stdlib-only: but wait — Ed25519 + SHA-256 only? The brief's priority #1 lists "Ed25519, SHA-256, HTTP, JSON, base64" as what's available. Encryption needs... X25519? ChaCha? AES? Hmm. "Implementable from a language's standard library: Ed25519, SHA-256, HTTP, JSON, base64." Strictly, most standard libraries (Go, Python, Rust std, JS WebCrypto) have more, but the brief names that set. Can we build encryption from Ed25519 + SHA-256 alone? 

- Key agreement: Ed25519 keys can be converted to X25519 (birational equivalence) — but X25519 scalar multiplication needs the Montgomery ladder implementation; is that "from the standard library"? It's implementable in ~50 lines of pure code from RFC 7748 using bignum arithmetic — stdlib has bignums in many languages (Python yes, JS BigInt yes, Go math/big yes). Hmm, "no dependencies" means no third-party libs; implementing X25519 from RFC 7748 text with stdlib bignums is allowed but strains "weekend implementable". Alternative: use Ed25519 signing keys to *sign* ephemeral key exchanges? No — you need DH for secrecy.

Alternative: derive shared secret via DH on Curve25519 implemented via the birational map from Ed25519. Converting Ed25519 keys to Montgomery form and doing X25519 is well-known (used by libsodium's crypto_sign_ed25519_pk_to_curve25519). Specifying that conversion in the text is doable (it's a few formulas: u = (1+y)/(1-y) mod p). Then:
- KEM: ephemeral X25519 keypair per item (or per audience), shared secret = X25519(eph_sk, recipient_pk_x), KDF = SHA-256(prefix || shared || eph_pk || recipient_pk) → content key. 
- AEAD from SHA-256 alone? We'd need a stream cipher: SHA-256 in counter mode (keystream = SHA256(key || nonce || counter)) gives a CTR-like stream cipher — this is a legitimate construction (KDF-as-stream-cipher), though unauthenticated; add MAC = SHA-256(key_mac || ciphertext) (HMAC-SHA-256, and HMAC is definable from SHA-256 in 10 lines). So: encrypt with SHA-256-CTR, MAC with HMAC-SHA-256, EtM. That's implementable from literally SHA-256 alone. It's nonstandard but sound if domain-separated properly. Is recommending a homebrew AEAD wise? For this design brief, the constraint forces it. I should be explicit: this is the price of "no dependencies," the construction is standard-pattern (CTR + HMAC EtM), and the spec fixes domain separation, key lengths, and nonce generation to remove implementer degrees of freedom. Alternatively I could note: "if your stdlib has XChaCha20-Poly1305 or AES-GCM, you may use it via a suite byte" — a cipher-suite escape hatch. Good: suite identifier field with suite 0 = the SHA-256 construction, suite 1 reserved. That keeps floor #1 and honesty.

Actually, do we even need asymmetric encryption? Recipients addressed "without publishing the audience": per-item, generate a random 32-byte content key `ck`; encrypt body under ck; then for each recipient, wrap ck: `wrap_i = HMAC-SHA256(KDF(DH(eph, recip_i)), "wrap") XOR ck`? Simpler: `enc_ck_i = ck XOR SHA256("wrap" || shared_i)`, publish (eph_pk, enc_ck_i) pairs without recipient identity — recipients trial-decrypt each wrap (a "trial decryption" scheme like Signal's sealed sender / PGP's hidden recipients). Cost: O(n) trial decrypts per item per recipient — fine for family scale (audience ≤ dozens). This hides the audience from the hub: hub sees an encrypted item with k wraps, learns size, timing, and k (number of recipients). That's the metadata leak to name honestly. Non-goal says hide *who* a message is for — trial decryption does that; hub learns *how many* and *when*.

But wait: recipients need a *encryption public key* separate from their signing key? Using the same Ed25519 key converted to X25519 for both signing and DH is generally discouraged (key reuse across primitives). Options: (a) card contains a dedicated encryption key (X25519 key generated by app, signed in the card) — cleaner, allows rotation, but adds a key type. (b) reuse signing key via conversion — fewer moving parts, weekend-friendlier, but cross-protocol risk: if some other protocol uses the same Ed25519 key for signing and an attacker can get decryption-oracle behavior... In our protocol, both uses are inside our spec; the risk is the "decryption triggers a signature" style cross-attack — Ed25519 signing of attacker-chosen messages happens when author signs items; the X25519 DH use is decryption. The known concern: converting Ed25519→X25519 and using both is what libsodium supports and many protocols do (e.g., some Matrix tooling, age's ssh-ed25519 plugin uses ed25519→curve25519 conversion for encryption to ssh keys). Given "minimal," reusing the identity key for encryption via conversion is tempting, but key rotation then rotates encryption too (fine, actually desirable), and theft of signing key = theft of decryption key (they're the same device-held secret anyway). The clean separation argument: if you want to give a hub or a device partial powers... we don't. The device holds everything (floor 3). So reuse the identity key via the birational conversion. I'll argue it: one key per person, ever; conversion formulas in the spec (4 lines); rejected alternative: separate encryption subkey in the card (more complexity, marginal security gain here; noted as the choice I'm "least sure of" candidate). Hmm, actually there is one real argument for a separate encryption key: forward secrecy / rotating encryption keys while keeping identity. But past ciphertext on a hostile hub with an old compromised key is decryptable anyway once key stolen; rotating encryption key protects past messages if old encryption keys are deleted — devices keep everything though. I'll keep one-key conversion but flag it.

Hmm wait, actually let me reconsider: theft scenario (section 5 asks rotation, loss, theft). With a single key, theft requires re-keying the identity = new identity essentially + social re-establishment. With a two-layer design (identity key + signed subkeys), you could rotate a compromised *device* key with the identity key kept cold... but Grandma must never handle keys. Scenario 2: she loses her phone; she's "back by calling her daughter." So recovery is social: she generates a fresh key on the new phone (app does it silently), and her daughter (and/or other pre-designated family members) vouch: a **recovery endorsement** — the new key is signed by M-of-N pre-designated "guardians" (family). Her old followers' apps see: old key's card expired (she can't re-sign it — lost), plus endorsements from people they trust pointing old→new? How does a follower map old identity to new? The endorsement must reference the old key. But the endorsement is signed by the daughter's key, which the follower already trusts... trust in *what sense*? We need "people you follow who also follow grandma" — web-of-trust-ish but minimal: an app can display "3 people you follow vouch that K_new continues K_old; accept?" One tap. Grandma's daughter "brings her back" by (a) adding her to family audience lists anew — audience membership is per-key, so encrypted content needs re-keying to the new key anyway (old encrypted content: grandma lost her device copy! Wait — floor 3 says the copy was always on her device; she lost the device. Her content survives on the hub (hostile? in scenario 2 the hub isn't hostile necessarily) and on other family members' devices who received her posts. Her private posts: encrypted to audience including herself; new key can't decrypt old content unless someone re-shares content keys. Daughter's app can re-wrap old content keys to grandma's new key — daughter has ck's for items addressed to family including daughter; she can re-wrap to grandma_new and send. Nice: recovery = social re-keying by the audience. That's a beautiful property of the wrapping construction: any audience member can re-extend access. State it.)

So recovery model: there is no secret backup (explicitly: "never told to store a file outside the house"). Identity continuity after loss is *social*, and the brief's non-goal 2 says continuity of a stranger's view across total key loss is NOT required. So: new key, guardians vouch, family re-adds to audiences, old content re-wrapped by those who hold it. "Back by calling her daughter" = daughter initiates a "re-key invitation": daughter's app sends (via any channel — a call! read a short code over the phone) ... hmm, how does the vouch get bound to the right new key without grandma handling keys? Daughter must learn grandma's new pubkey: over the phone they read a 6-8 word/char code = truncated hash of the new key + the new hub URL spoken/typed? "Never shown a key" — a spoken 6-word code isn't "a key" UX-wise; it's a call-your-daughter ritual. Or simpler: daughter is physically present or they use an in-app "invite" link sent by any messenger: grandma taps a link daughter sent (link contains daughter's key+hub), grandma's app posts her new card to her hub and sends an authenticated hello to daughter; daughter's app shows "Grandma (new phone) — is this her? confirm by the call you just had" → confirm creates vouch. The out-of-band check is the phone call itself. I'll spec the vouch artifact: `{kind:"vouch", old_pk (optional, if known), new_pk, note, sig by guardian}`. Apps auto-accept vouches from N≥1 people-you-follow for display continuity ("Grandma (2)"), and importantly re-key content.

Key *rotation* (planned, no loss): signed **rotation statement** by old key endorsing new key, published on old hub(s) and new; followers auto-follow the chain. Rotation chain = the "successor" pointer in card: card field `succ: <pk_new>` signed by old key; and new card contains `pred: <pk_old>` + proof (sig by old key embedded). Theft: old key signs succession to attacker-controlled key — contested. Contested departure (the ex claims her departure relocation is forged): relocation is signed by *her* key; he can't forge it. The contested case is the reverse: *he* serves a frozen old card and claims *her new* relocation is a forgery. Readers decide by signatures: her relocation statement verifies under the key they've always known — it's self-evidently authoritative; his claim is unverifiable assertion. Also the expiry mechanism: his frozen copy expires. And her new card's `pred` chain links to the old history hash, so continuity of *content* is provable; the fork evidence (two cards same seq different content) is publishable proof of hub misbehavior. For theft-contested-key: if her key is stolen, thief's rotation and her recovery-vouch compete; resolution is social (guardians, expiry, out-of-band); protocol's job is to make both claims visible and verifiable, not to adjudicate. Name that honestly.

**Floor 4: cross-hub without access-control agreements.** This is where the encryption design pays off: access control is *cryptographic*, not hub-enforced. A on a.example posts family-only item: body encrypted with ck, wraps for each family member's pubkey (including B on family.example). Item is hosted on A's hub *publicly fetchable* (ciphertext). B's reader polls A's hub (B follows A), fetches items, trial-decrypts wraps with B's key. Hub a.example learns nothing about audience. Replies: B posts reply item on *B's* hub, encrypted to same audience (or to author + family), with `in-reply-to: <item hash>` and also copies it to A via an **inbox drop**: A's hub exposes `POST /u/<pk>/inbox` accepting signed+encrypted items addressed to A; A's reader polls its inbox endpoint (or hub notifies). But the hostile hub controls "any inbound message path" — it can drop B's reply delivery. Floor 4 must work anyway: readers also fetch replies *from the replier's own hub*. Thread discovery: A's item says nothing about future replies; B's reply references A's item hash. How do *other* family members (C on a third hub) see B's reply? C follows B too (family all follow each other), so C polls B's hub, sees reply, follows hash ref to A's item. So the thread is a DAG woven across hubs by hash references; each reader assembles locally. "With no access-control agreement" — indeed none: everything is encrypted to keys, and transport is plain HTTPS GET of ciphertext. The reply/reaction crossing the boundary = an item on the replier's hub + optional inbox copy for notification latency. Inbox is an optimization, not the mechanism. That's the clean answer. Hub cost per identity flat: hub serves static signed blobs; no per-recipient computation. 

Reactions: same as replies, `kind:"react", ref:<hash>, body:"❤"`.

**Public interop (floor: RSS/fediverse)**: hub serves `/u/<pk>/feed.json` (JSON Feed) and `.rss`/`.atom` generated from public items. Fediverse: a bridge is "an extension, not a requirement": the spec notes a bridge subscribes via feed and re-posts to ActivityPub; replies coming back is out of scope. Bluesky: similar via feed→ATProto bridge. Nothing in the core spec; just ensure public items map cleanly (title, html content, published, id, url, attachments). Keep item payload fields aligned with JSON Feed vocabulary to make the mapping trivial: `body_html`, `summary`, `attachments`. Good: "the unsigned rendering is derived; the signed item is primary."

**Fetch flow (first contact & poll)**: 
- First contact: given `https://hub.example/u/<pk>` (from an introduction), GET `/u/<pk>/card` → verify sig, expiry, pk matches URL. Card lists: hubs (mirrors), feed endpoints, seq, prev-hash, encryption key (= identity key), display name/avatar (self-asserted, signed). Then GET `/u/<pk>/items?since=0` → verify each.
- Poll: GET card (check seq/expiry/succ), GET items since last seen seq. Items need per-author seq for gap detection: author assigns monotone `n` to each item; gaps = hub withholding. Withholding detection: seq continuity in the signed payload! Host can freeze (serve up to seq k) but cannot show a gap-free fabricated stream; freezing is detectable later via card expiry + seq compare across sources. Also items can be fetched from *any* mirror: readers can exchange items peer-to-peer? Keep it: any hub may mirror any author's signed items (they verify), so a friendly hub can serve her archive after she leaves; the divorce scenario: she leaves, her new hub (mom's closet box) imports her device archive, readers redirect via relocation statement cached in their apps and findable at old hub (if he serves it — he may refuse; then readers already saw the relocation in the card they polled before, or get it from her new hub once they learn it — circular; break the circle: readers poll *multiple* remembered hubs for the card; she pre-announces: the moment she decides to leave, her app publishes the relocation card (seq N+1, new hub) to the old hub *and* to every follower via the inbox mechanism — followers' apps store it permanently. Even if he deletes it later, followers have it. New followers post-move get the new URL socially.)

**Inbox**: `POST /u/<pk>/inbox` with a signed item whose wrap includes the recipient. Hub stores by hash, serves `GET /u/<pk>/inbox?since=` to the owner only? Hub can't authenticate owner without keys... it *can*: GET with a signed request? Adds request signing — complexity. Alternative: inbox items are encrypted to the recipient anyway, so hub can serve inbox to *anyone* without confidentiality loss; owner just polls and trial-decrypts. Serve inbox publicly (ciphertext blobs) — simplest, hides nothing more. Hub can still censor (it's the inbound path — hostile). Mitigation: senders also keep replies on their own hubs (they do anyway); the inbox is just latency. State that.

**Spam**: open POST endpoint = spam magnet. Mitigations without global state: hub may require (a) items signed by a key the recipient follows (hub can't know recipient's follow list... it could if recipient publishes it — follow list could be public-ish or hub-config), (b) hashcash-ish: no, complexity, (c) rate limits per IP, (d) recipient-supplied allowlist served to hub as config (simple JSON the owner signs and uploads: "accept inbox items signed by these keys"). For family scale: allowlist = people I follow + their vouched successors. Fine, specify optional `inbox_policy` signed doc. Flag as soft.

**What the hub learns anyway (assurance 2 honesty)**: timing, sizes, wrap-count per item, IP addresses of readers (unless readers fetch via other hubs), social graph via follow fetches (which hubs get polled by whom), inbox traffic. Padding/batching: optional, non-goal beyond hiding recipients. Also: hub learns who reads public content; nothing to be done within scope. State it.

**Alteration/backdating protection**: items signed; timestamps are author-asserted (host can't change without breaking sig) — but author can backdate their *own* posts; fine, non-adversarial (the adversary is the host). The "alter or backdate what she wrote" — host can't alter (sig) and can't backdate new items as her (sig key). Monotonic seq prevents silent reordering; hash-chain per author log? Do items chain to each other (prev item hash)? That gives the author-log fork evidence like the card chain. Cost: author must know last item hash — device has the copy, fine. Include `prev` in item payload: chain head in card. Then a hub that withholds a middle item creates an evident gap; serving a fork is cryptographic proof of misbehavior. This is cheap and powerful: **the author's outbox is a hash chain, head committed in the card**. Weekend-implementable? It's one extra field + one rule (prev = hash of previous item or "0" for first). Yes, include. This gives: integrity of order, detection of withholding (gap in chain), equivocation proof. Staleness of a frozen archive still relies on card expiry. 

**Architecture one-pager** (deliverable #1): artifacts:
1. **Card** (signed, expiring): identity binding — pk, hubs, seq, prev_card_hash, display name, avatar ref, succ pointer (optional), policy refs (inbox policy). Endpoints: `/u/<pk>/card`.
2. **Item** (signed, hash-chained): note/article/photo/reply/reaction/repost/delete? (delete = tombstone signed; honored by honest hubs, unenforceable on hostile hub — name it; "cannot unpublish from adversary" honest cost).
3. **Wrap/encryption envelope** inside item payload when audience-restricted.
4. **Relocation** = just a card with new hubs + `moved:true`? Or separate artifact? A card with updated `hubs` suffices; `pred`/`succ` fields for key changes. Keep artifacts minimal: card + item + (vouch as item kind) + (inbox_policy as signed doc served at well-known). Vouch as item kind "vouch" published by guardian. Good — four artifact types: card, item, policy, and feed renderings (unsigned, derived).

Reader first contact & poll (already described). Publisher writes: create item → append to chain → upload item + updated card via authenticated POST. How does the hub authenticate the publisher? Items and cards are self-authenticating (signed); hub needs *no* separate auth — it just verifies signatures match the URL's pk before accepting. Beautiful: **hub accounts need no passwords**; the write endpoint is `POST /u/<pk>/items` accepting only valid signatures from pk. Rate-limiting and quota are hub policy. This also means migration is trivial: new hub accepts her signed blobs immediately; no account setup ceremony beyond storage quota (out of band — commercial hub: payment; family hub: dad creates directory). State: "authentication = possession of the key, proven per artifact; there are no accounts."

**Encoding shapes** (concrete bytes):

Card payload JSON:
```json
{
 "v":1, "type":"card", "pk":"<b64url>",
 "seq":41, "time":"2026-04-25T12:00:00Z",
 "exp":"2026-07-25T12:00:00Z",
 "prev":"<b64url sha256 of previous card payload or null>",
 "hubs":["https://newhub.example"],
 "name":"Mara", "avatar":"<item hash or https URL>",
 "head":"<b64url sha256 of latest item payload or null>",
 "succ":null
}
```
Envelope: `{"payload": "<b64url of those exact bytes>", "sig":"<b64url Ed25519 over ("FP1"||payload)>"}`. Domain separation: signatures over `"fp/1 " || payload` string. Hash id of an artifact = b64url(sha256(payload bytes)) — maybe prefixed multihash-lite: `sha256:<...>`. Keep `sha256-` prefix? Simplest: all hashes are SHA-256, say so once, bare b64url everywhere.

Item payload:
```json
{
 "v":1,"type":"item","pk":"...","n":117,"time":"...",
 "prev":"<hash of item n-1 payload>",
 "kind":"note|article|photo|reply|react|vouch",
 "refs":["<hash>",...],
 "aud":"public"|"sealed",
 "body":"<html or text>"           // if public
 // if sealed:
 "eph":"<b64url X25519 eph pub>",
 "wraps":["<b64url 32-byte wrapped ck>",...],
 "ct":"<b64url nonce||ciphertext||tag>"
}
```
ck = random 32B. `shared_i = X25519(eph_sk, conv(pk_i))`; `wrap_i = ck XOR SHA256("fp/wrap"||eph||shared_i)`? Include eph in KDF: `SHA256("fp-wrap-v1" || shared_i || eph_pub)`. Body keys: `k_enc = SHA256("fp-enc-v1"||ck)`, `k_mac = SHA256("fp-mac-v1"||ck)`; nonce 16B random (128-bit random nonce collision-safe at family scale — state birthday math: 2^64 safety margin far above item counts); ct = body XOR SHA256-CTR(k_enc, nonce); tag = HMAC-SHA256(k_mac, "fp-tag-v1"||nonce||ct) truncated? Keep full 32B tag — simpler, no truncation debate. Recipient trial: for each wrap, compute candidate ck = wrap XOR SHA256(...), then verify tag with candidate-derived k_mac — one HMAC check per wrap; false-positive probability 2^-256, fine. Note: no recipient index → hub can't correlate which wrap is whom across items... actually hub *can* correlate: if the same audience reused, are wraps linkable across items? Each item has fresh eph → shared differs → wraps unlinkable. Good: fresh ephemeral per item gives unlinkability of audience across items. But eph key reuse must be forbidden: state MUST be fresh random per item.

Multiple items same audience efficiency: fine, per-item overhead 32B/recipient — trivial.

Groups/audience management: "family" audience = the set of keys she chooses per post; app defaults to "my family list" (local). No group artifacts needed. Replies to sealed items: encrypt to the same audience — replier knows audience because they could decrypt (they know who the other recipients are? No! Wraps hide recipients *from everyone*, including other recipients — trial decryption tells B only "I am a recipient," not who else. So B cannot reply-all exactly. Options: (a) put recipient list inside the encrypted body (visible to audience only) — yes! The cleartext body JSON includes `audience: [pk,...]` so recipients can reply-all; hub sees only count from wraps. Audience-hiding-from-outsiders preserved; insiders see each other (they're family — fine, and the adversary-inside-audience is explicitly out of scope: "no confidentiality mechanism defeats someone inside the audience"). Include `audience` in cleartext. Also allows future re-wrapping by any member (recovery re-key) — they know who to notify, though re-wrapping needs each member's key (public, in cards) — fine.

Photo handling: body too big for JSON? Photo item kind with `attachments`: encrypted blobs stored as separate objects `GET /u/<pk>/blob/<hash>` where blob = encrypted bytes, and item contains hash + key? If blob is encrypted with its own random key and the item (sealed) carries that key, hub stores opaque blobs; sizes leak (photo dimensions approx). Public photos: plaintext blob. Blob addressing by hash; dedup free; hub can garbage-collect unreferenced blobs. Keep blob spec to one paragraph: `PUT /u/<pk>/blob` → served at `/blob/<sha256>`; item references `blob:<hash>` plus `enc_key` if sealed. This keeps item JSON small.

**Reader poll efficiency**: `GET /u/<pk>/items?after=<n>` returns items n>k in order; hub cost flat per identity (static files, could be literally a directory of files named by n!). The "dumb hub" (scenario 6): a static file server + a tiny CGI for POST verification... or even dumber: publisher writes files via any file transfer (SFTP, S3, rsync) — the protocol's on-the-wire read format is just static JSON files! Wow, that's the weekend hub: "a hub is a static directory; the dynamic part (verifying POSTs) is a convenience shim; publishing to your own static hosting works because cards/items are self-certifying." I should include that: hub = 4 endpoints, 2 of which can be static. Dumb hub implementation: nginx + a 100-line POST verifier. State per-identity: directory `/u/<pk>/` with `card`, `items/<n>`, `blobs/<hash>`, `inbox/<hash>`.

**Floor-by-floor delivery (deliverable #3)**:

1. Host cannot speak for you: every artifact signed; readers MUST verify before attributing; URL contains pk so introductions bind content to key; hub has no key (authentication = signatures, no shared secrets with hub — so hub compromise yields zero forging ability). Failure mode: a reader implementation that skips verification for convenience (e.g., plain RSS consumers) can be lied to — named honestly; the assurance holds only among verifying clients. Also: hub can serve *old* valid artifacts (replay) — mitigated by card expiry + seq.
2. Host cannot read: sealed items, ck wrapped per recipient, eph DH, audience never in clear; hub learns count/size/timing. Failure modes: malicious recipient (out of scope), endpoint compromise (device stolen = game over, see theft), traffic analysis (non-goal), hub correlating posts-to-timing with real-world events (acknowledged). Also chosen-plaintext-ish: hub knows approximate family size → guesses audience set, can't confirm (wraps unlinkable, no recipient tagging) — but *reply patterns* leak: when B replies publicly-referencing a sealed item, hub infers B was in audience. Mitigation: replies to sealed items should themselves be sealed and reference the parent hash *inside* the encrypted body?? But then thread assembly requires decryption — fine, readers in audience can assemble; outsiders see sealed blobs. But floor 4 scenario says "a reply and a reaction cross the hub boundary" — they're sealed too, fine. However `refs` in the clear enables discovery of replies for readers who *can* decrypt... if refs are inside ciphertext, how does anyone find B's reply? They poll B's items anyway (they follow B); the ref just links it. So move `refs` inside the sealed body for sealed items (clear refs only for public items). Then outsiders can't even see that item X replies to item Y. Better privacy, same function. But wait: does the *inbox copy* need the parent ref in clear for the hub to thread? No — hub does no threading. OK: sealed items carry refs inside ciphertext. Note tradeoff: public discovery of sealed threads impossible (desired).
3. Host cannot keep you: key on device, full local archive (app stores everything it publishes — "outbox = source of truth"), relocation = new card signed anytime (no cooperation needed), followers notified via inbox + next poll; old hub may freeze/delete — expiry marks frozen copies stale; deletion is fine (she has the archive; mirrors/family hubs can re-serve it: any hub can host her blobs since they verify). Failure modes: (a) all followers lose the relocation (only if she never published before vanishing and they never polled — mitigate: apps cache every card seen; relocation sent via inbox to every mutual); (b) old-URL strangers find stale-but-valid archive — expiry makes it read stale; that's the designed behavior, not a failure; (c) commercial hub holds her domain name (her own domain as hub): identity is key, domain is just a hint — moving domains = publishing new card. Scenario 4 solved by the same mechanism: `family.example` dies → everyone re-hubs; readers find them via cached relocation cards + any surviving mirror; where the whole domain vanishes with no warning and readers have no cached card: out-of-band re-introduction (a family group chat). Named honestly: **there is no global directory; total disappearance of all hubs without prior relocation requires out-of-band rediscovery.** That's the price of no-registry priority. Mitigation: apps SHOULD keep N previously seen cards per follow and SHOULD accept relocation statements from any source (they're self-certifying) — "relocation gossip": when a reader learns pk's new hub, it can serve the card to others; family apps naturally exchange.
4. Cross-hub: described above; no agreements because hubs never make access decisions — cryptography does. Failure modes: hostile hub censors inbound inbox (mitigated by polling sender hubs directly); hostile hub serves different items to different readers (chain + cross-checking: readers fetch from multiple hubs; also readers can compare head hashes via friends); latency (polling interval tradeoff; inbox = optional push).

**Scenario walk-throughs (deliverable #4)** — write 1 (divorce), 2 (grandma), 3 (two hubs), 4 (domain goes) concretely with artifact flows. Also touch 5/6/7 briefly.

Divorce: Mara on ex Dan's hub. Her posts signed by her key (device-only) — Dan can't forge (floor 1). Family-only posts sealed to keys — Dan reads only public items (floor 2) though he *is* in "family" audience? The scenario says he's "inside the family — entitled to see family-only content." Hmm — the brief says he's inside the audience, so he *can* read family content; floor 2 is about content *not addressed to him*: after the divorce she posts to family-minus-Dan: new wraps exclude his key; he cannot read those (he's now outside that audience). Her archive: device copy (floor 3). She relocates: signs card seq 42 with hubs=[mom's box], prev=hash(card41), pushes to Dan's hub (he may delete it — her app already sent it to followers' inboxes) + uploads full archive to mom's box. Dan's frozen copy: card expired 90 days later; readers label stale; its head hash ≠ her live head; if Dan keeps serving old items, they verify but are visibly behind (seq/head/expiry). Dan alters/backdates: impossible (sig). Dan claims her move is the forgery: her card42 verifies under the same pk with correct prev chain — self-proving; his competing card (if he forges one) won't verify at all; if he replays card41: expired/older seq. Mother's app: one tap — it received the relocation (inbox) or on next poll sees... wait, if Dan refuses to serve card42, mother's app polling Dan's hub sees only frozen card41 expiring; but her app had pushed relocation to mother's inbox *before/while* leaving, and the app stored it: "Mara moved → follow her at new hub? [yes]" — one tap. Also gossip: mother's app can ask Mara's brother's app... keep to: inbox delivery + cached cards.

Grandma: app generates key silently at first launch; name picked; never sees key; publishes to family hub (daughter's hub, say). Phone lost a year later: new phone, app generates new key; she "calls her daughter": daughter sends an invite link (any channel) containing daughter's card ref; grandma taps; her app publishes new card to hub and POSTs a hello (signed by new key) to daughter's inbox; daughter's app: "New key claims to be your mother — you just spoke; confirm?" → daughter signs vouch {old→new}. Family apps that follow daughter (and follow grandma_old) surface: "Daughter vouches this is Grandma's new key — accept?" One tap each. Content: grandma's old posts were sealed to family incl. herself_old — new key can't read them; daughter's app (member of those audiences, holds cks in its local store) re-wraps each ck to grandma_new and POSTs re-wrap bundles to grandma's inbox; her app merges them into its archive: photos and writing back. Her *own* hub archive: also still on family hub (daughter re-serves). What's lost: items addressed to audiences where no surviving member is reachable — acknowledged edge. Also her new key can't *prove* continuity cryptographically (old key lost, can't sign succ) — the vouch web + expiry of old card is the continuity story; strangers see a new person (non-goal 5, accepted). 

Two hubs: A seals family post: ck, wraps for {A_self, B, C...}; item on a.example. B's app polls a.example (B follows A — follow = local list of card URLs), trial-decrypts, shows. B replies: sealed item on family.example with refs inside ct → parent hash; wraps to same audience (from cleartext `audience` field); *also* POSTs copy to A's inbox on a.example for speed. C (on hub3) polls both A and B, assembles thread by hash. No hub configured anything. Reaction: same with kind react. Access control: purely the wrap set. Hub a.example sees: sealed item #117 from A (48 wraps... e.g., 6), inbox POST from family.example IP at t+3min (infers B may have replied — traffic metadata, admitted).

Domain goes: family.example unaffordable. Each person: signs new card (new hub), app pushes relocation to all followers' inboxes on *other* hubs (if family.example is dying, inbox there may be dead — but followers poll their *own* hubs' inboxes; sender POSTs to recipient's *current* hub, wherever that is — recipients on family.example too... if it's fully dead, recipients can't receive; rely on: their apps poll *all known hubs* of each follow incl. old ones; cards have 90-day expiries so everyone re-signs monthly — the dying period is covered by pre-announcement: sensible UX: "hub going away" workflow months ahead. Worst case: out-of-band (the family chat) with a new introduction URL. Honest about it.)

Big lazy hub: per-identity cost = static files + signature check on write (one Ed25519 verify per upload — microseconds); read = nginx; flat. Hostile-at-scale: can't forge/read/keep; can delete (users keep archives; mirrors); can extort ("pay or I delete"): exit is free, so extortion fails; can surveil metadata (admitted).

The stranger: public items → hub renders JSON Feed/RSS at `/u/<pk>/feed.json` (derived, unsigned); feed reader subscribes. Mastodon: a bridge (separate software, reads feed, posts to AP; replies back = extension via bridge writing to inbox — noted as extension). Key loss: author re-meets — new key, old card expires, stranger re-subscribes socially; no cryptographic continuity (non-goal). Fine.

**Key change & recovery (deliverable #5)** — rotation (both keys alive): card succ + dual-signed transition; items continue on new key with `pred` linking old head hash → chains merge; readers auto-accept rotation (no tap needed) since it's cryptographic. Loss: vouch-based (above). Theft: race — thief rotates (has key), victim vouch-recovers; both claims visible; tie-breakers: (a) victim's old content chain head vs thief's new items (thief *can* continue chain — has key! So thief's continuation is cryptographically valid — the protocol cannot distinguish; it surfaces both; humans adjudicate via vouches/expiry/out-of-band). Explicit: **key theft is indistinguishable from key ownership at the protocol layer; the design's answer is visibility + social adjudication + the ability to walk away and be re-followed.** Contested departure: covered (divorce). Also compromised *hub* claiming relocation forged: readers trust signatures only.

**Encryption construction (deliverable #6)** — detail as above. Also: what's encrypted — the full body JSON including refs, audience, kind? kind maybe in clear for hub indexing? Hub needs nothing; keep kind inside ct for sealed; but readers trial-decrypt every sealed item from everyone they follow — cost trivial (one HMAC per wrap). Include `n` and `prev` in clear or inside? The chain (n, prev) is metadata hub could use to withhold detection... chain must be verifiable by recipients only? If chain fields are inside ct, outsiders can't even see seq — better privacy; but gap detection then only by audience members. Public items: chain in clear (needed for interop audits). Sealed: put n/prev both inside AND... hmm, if n inside ct, a reader can't even order sealed items before decrypting; they decrypt all anyway. Keep sealed items fully opaque except {pk, eph, wraps, ct, sig}: maximal hiding, still verifiable authorship (sig outer). Author chain for sealed items lives inside ct; recipients verify chain post-decryption. Hub learns: author, time (arrival), size, wrap count. Good, that's the honest leak list. Wait — outer sig over payload including ct — hub can verify authorship (fine, it's her hub account) and so can mirrors.

One more: forward secrecy? None (non-goal-ish; device theft exposes old cks anyway since app stores them). State it.

Chosen construction summary for spec text:
- conv(Ed25519 pk → X25519): standard birational map: u = (1+y)/(1−y) mod 2^255−19; include formulas + note that sign byte is dropped; secret key conversion: h = SHA-512?? — wait, Ed25519 secret scalar derivation uses SHA-512 (that's how the Ed25519 secret expands). Problem: brief says SHA-256 only! Ed25519 itself internally uses SHA-512 per RFC 8032! Hmm. The brief lists Ed25519 as a primitive — the library's Ed25519 handles SHA-512 internally; that's "Ed25519" as a black box. But for key conversion to X25519, libsodium uses SHA-512 of the seed too. To stay within "Ed25519 + SHA-256" black boxes: derive X25519 secret as SHA-256("fp-x25519-v1" || ed25519_seed)?? That breaks compatibility with standard conversion but we don't need compatibility — self-contained protocol! X25519 sk = SHA-256(domain || seed) then clamp per RFC 7748. X25519 function itself: needs implementing Montgomery ladder (~40 lines with bignum) — provide pseudocode in spec appendix (RFC 7748's ladder is short). Is that weekend-feasible? RFC 7748 includes full ladder pseudocode; copying it with stdlib bignum is a known exercise. Acceptable; name the tension: this is the one place "no dependencies" bites, and the alternative (require libsodium/age) violates priority 1. Also offer suite escape: implementations MAY use any AEAD if both ends agree via card `suites` field? No — interop fractures; better: spec fixes suite 0; if your stdlib happens to have X25519+ChaCha (Go? not std; Python no; WebCrypto has ECDH P-256 + AES-GCM...) — resist; one suite, done. Flag in cost sheet: homebrew-ish AEAD (SHA-256 CTR + HMAC) is the piece I'd most want real cryptographers to review; the primitive set forced it; if the constraints allowed one more dependency it'd be XChaCha20-Poly1305.

Hmm, actually — reconsider: is there a simpler *encryption-addressing* scheme? Alternative: symmetric-only "family key" shared out of band (a group symmetric key per family, rotated socially). Simpler (no DH at all!): each family shares a 256-bit group key, distributed once via... how? In person / via sealed 1:1 messages — which need DH. Circular; keep DH. Also group keys can't exclude Dan post-divorce without re-keying everyone and Dan's old wraps still decrypt old content (fine, he was audience) — but membership changes with group keys are painful; per-item wrapping handles per-item audiences naturally ("just the sisters"). Keep per-item DH wrapping.

**Honest cost sheet (deliverable #7)**:
- No global namespace/discovery: introductions are social (URLs with embedded keys); search doesn't exist; total hub loss without pre-relocation = out-of-band rediscovery.
- Metadata: timing/size/wrap-count/IPs visible to hubs; follow graph partially visible.
- No deletion guarantee (hostile hub keeps ciphertext/stale public copies; expiry labels staleness but old public content remains readable and attributable — she can't un-say; "stale" is the best label available).
- Key theft indistinguishable from ownership.
- Deniability: none — signatures are non-repudiable; family context might prefer deniable; named as a choice (rejected OTR-style deniable auth: complexity, and signing is the point of floor 1).
- Forward secrecy: none.
- Homebrew AEAD: flagged.
- Re-publishing/mirroring of someone else's items by hostile parties (they can re-serve your public content forever, validly signed — it's yours, that's the point).
- Reader must verify: interop feeds are unverified (RSS consumers trust hub).
- Inbox spam.
- Storage: every device holds full archive — fine at family scale; not for millions of items (non-goal).
- Clock: author timestamps self-asserted; no trusted time; backdating by self possible; ordering relies on seq not time.
- Multi-device: same key on two devices → chain forks if both publish simultaneously (prev chain!). Mitigation: one-writer rule or per-device seq with merge — flag as under-specified; simplest: spec says apps MUST serialize via primary device or accept fork-then-heal (publish merge item listing both heads). Least-sure choice #1.
- Least sure: (a) single-key dual-use conversion; (b) expiry-based staleness UX (90 days; what if she's offline 6 months? Cards expire → "identity lapsed" display vs hostile freeze indistinguishable — hmm! If grandma doesn't open the app for 4 months, her card expires and her content reads stale — false positive. Mitigation: long expiry (e.g., 400 days) + app auto-renew on open + hub MAY auto-... no, hub can't re-sign. Tension between staleness-detection speed and offline tolerance. Pick 400 days? The divorce wants frozen copy to read stale quickly; she can set short expiry when leaving (her final card on old hub: exp soon). Actually clever: expiry is author-chosen per card; default 400d; when relocating she issues old-hub-final card with short exp. Good compromise — state it.)

**What to cut if halved (deliverable #8)**: cut: inbox endpoint (rely on polling sender hubs), blob storage (photos as base64 in items or external URLs), inbox_policy, vouch artifact (recovery = re-introduction socially), hash-chain prev (keep seq only), reaction kind (use reply with emoji), JSON Feed rendering (keep RSS only)... The brief asks what *I* would cut: I'd cut in order: (1) feed renderings→single JSON Feed template (interop shrinks but RSS bridgeable), hmm — actually interop is priority 3; halving spec: cut inbox+policy (floor 4 survives via polling), cut blobs (inline base64 photos), cut card prev-chain (keep seq+expiry), cut item prev-chain? The chains give withholding/fork evidence vs hostile hub — that's the adversary center... but floor doesn't explicitly require fork detection; staleness via expiry covers divorce. I'd keep card expiry+seq and item seq, cut both prev-chains if forced — reluctantly, noting fork evidence dies. Keep encryption (floor 2) and signatures (floor 1) and relocation (floor 3) and nothing-cross-hub-special (floor 4 is emergent). State: the irreducible core ≈ cards + items + sealing; ~half the text is already those.

Also deliverable #2 (identity primitive, argue alternatives): chosen = bare Ed25519 public key, introduced as key-in-URL (`https://hub/u/<pk>`), display names are local petnames + self-asserted signed name. Rejected: (a) DNS-based identity (user@domain) — dies with domain (scenario 4), owned by host (floor 3 fails); (b) did:web — same problem, plus DID machinery; (c) did:key + global resolution — no registry allowed, and key-only URIs can't be fetched (no location); key-in-URL is did:key *with* a location hint, self-certifying; (d) blockchain names — non-goal; (e) TOFU keyservers — registry again. Also argue: petnames, not global uniqueness — names are labels others assign ("Mom"), matching how families actually work; self-asserted `name` field is a hint, displayed with provenance.

Signature encoding details: envelope `{"payload": "<b64url>", "sig": "<b64url>"}`, sig = Ed25519.Sign(sk, "FP1/" || payload_bytes). Prefix domain-separates from other uses of key... note: if we also use key for X25519 conversion, cross-protocol consideration: DH use can't be tricked into signing; signing oracle could sign "FP1/..." messages only via app. OK.

Timestamps: RFC 3339 strings (stdlib-parseable everywhere). Base64url no padding (RFC 4648 §5) — one sentence.

IDs: artifact id = b64url(sha256(payload_bytes)) — call it `h`. Item references by h; URL scheme for refs across hubs: `fp://<pk>/<h>`? Fetching a ref: ask the author's hub `/u/<pk>/items/<n>` or by-hash `/u/<pk>/item/<h>`. Keep by-hash endpoint for replies-to-fetch: GET /u/<pk>/item/<h>.

Reader poll algorithm (concrete): for each follow: GET card; if sig bad → alert "serving invalid data" (host misbehavior visible!); if seq ≤ cached && not expired → nothing; if new hubs → update; then GET items?after=<max n seen> (public) and sealed items list (how does reader list sealed items? same endpoint; sealed items also have n in outer? I put n inside ct... then ?after doesn't work. Compromise: outer envelope carries `n` in clear (hub needs it to serve; it's metadata hub already knows as storage operator — it numbered the files). Chain prev inside ct. So hub learns seq numbers (it would anyway — it stores them). OK: outer envelope: {payload, sig}; payload for sealed item: {v,type:"sealed",pk,n,eph,wraps,ct}; inner cleartext (decrypted): {v,kind,time,prev,refs,audience,body/attachments}. Public item payload: {v,type:"item",pk,n,kind,time,prev,refs,body...}. Slight duplication but clean. Time for sealed: inside only → hub learns arrival time anyway. Fine.)

What does a reader fetch on first contact: card; then items?after=0 (paginated); blobs as referenced. On poll: card (compare), items?after=<n_max>, inbox (own): GET /u/<pk>/inbox?after=<t> — inbox items are sealed items addressed to me, stored by hub; hub learns they're for me (it's *my* inbox) — the wrap already implies maybe; inbox inherently reveals recipient=me; acceptable (hub operates the inbox). Non-goal says hide *who a message is for* — inbox items are for the mailbox owner, inherently. But sealed items in the main feed: recipients hidden. OK.

Publisher upload protocol: PUT/POST endpoints with the signed artifacts; hub verifies (pk in URL == pk in payload == sig valid; n == head n+1; prev == current head hash — hub enforces chain continuity! nice: honest hubs reject chain breaks, making withholding visible at write time... withholding is about *old* items though; fine). Hub stores. Hub as dumb as possible: verification is ~30 lines.

Now: "alter or backdate what she wrote" — her item times are self-asserted; chain order is authoritative; also card head hash pins the log tip each time she re-signs card (cards expire → re-signed monthly → periodic timestamped commitment, though time is still self-asserted; but *sequence* of card seqs with exps gives coarse not-before/after: card41 (exp July) references head h → h existed before July-ish... weakly; honest hub timestamps on receipt could add hub-attested time but hostile hub lies. Accept self-asserted time; state it.)

Also mention: mirrors/replication — any hub MAY serve any pk's directory (content-addressed, verifiable); "shadowing" a family member's directory is one rsync; this is what makes floor 3 robust and scenario 5's deletion toothless. And readers can treat multiple hubs as equivalent sources.

Sitemap of my answer (per deliverables):
1. Architecture one page (artifacts table + fetch flows).
2. Identity primitive + rejected alternatives.
3. Floor assurances mechanism-by-mechanism w/ failure modes.
4. Scenarios 1–4 (+brief 5–7).
5. Key change & recovery incl. contested departure.
6. Encryption construction implementable detail.
7. Honest cost sheet.
8. Halving cuts.

Plus: envelope/wire shapes early (concrete bytes), the "no canonicalization" rule, hub API table (dumb hub), and a short "does an existing protocol solve this?" note: ActivityPub (server-centric identity, host can speak for you — fails floor 1/3), AT Protocol (close: DIDs + signed repos! but PLC directory = global registry dependency; did:plc relies on a central PLC server; also complex: MST, CAR files — fails priorities 1/2; though did:web variant exists; worth crediting: ATProto's signed repo + rotation is the closest existing art; Nostr (keys = identity! but no encryption model for groups until NIP-17ish, relay-centric, no portability story because identity has no location hints and discovery is relay gossip; close on floor 1/3, weak on 2/4, plus no canonical bytes rule problems), Secure Scuttlebutt (signed append-only logs, key identity — very close spiritually! but not HTTPS/stdlib-simple, no web interop, discovery via pubs/gossip), indie web (Webmention/POSSE — DNS identity, fails floor). I'll write a compact "prior art and why not" paragraph — the brief explicitly invites "if an existing one solves it, say so" — my honest answer: ATProto and SSB each solve parts; none hits all four floors with stdlib-only; the design is basically "SSB's self-certifying log + ATProto's portability idea, flattened onto static HTTPS with keys-in-URLs." Credit them.

Naming: call it something — "Family Post"? "Hearth"? Brief says not asked for name; a short handle helps readability: I'll call it **FP** (family post) in byte prefixes: domain sep "fp1/..." Fine.

Length: aim ~4000-5000 words, dense, argued. Use headers, small JSON blocks, tables sparingly. Format per deliverable structure. Markdown headers, code blocks for byte shapes. Keep the one-page architecture actually ~one page.

Let me also double-check the X25519-from-seed clamping detail: RFC 7748: decode scalar: for X25519, take 32 bytes, clear bits 0-2 of byte 0, clear bit 7 of byte 31, set bit 6 of byte 31. Then Montgomery ladder base point u=9. Public key conversion from Ed25519 pk: edwards y → u = (1+y)(1−y)^{-1} mod p. Secret side: since we derive x-sk = clamp(SHA-256("fp1/x25519"||seed)), its public = X25519(x-sk, 9), which will NOT match conv(ed-pk) unless we use the standard ed-sk derivation (SHA-512(seed) clamp). Inconsistency: recipients encrypt to the public key in my card. If card publishes *identity* Ed25519 pk only, senders convert ed-pk → x-pk via (1+y)/(1−y); then my secret must correspond: standard conversion derives x-sk = clamp(SHA-512(seed)[0:32]) which matches. But SHA-512 isn't in our allowed set... Ed25519 library internally does SHA-512 but doesn't expose it. Options: (1) derive x-sk = clamp(SHA-256("fp1/dh"||seed)) and publish x-pk explicitly in the card (two public keys in card: sig pk in URL, dh pk in card — signed, so tamper-proof). This avoids the birational conversion AND the SHA-512 issue, at cost of one extra 32-byte field in the card. Cleaner! The card's dh key is signed by the identity key → bound. Rotation can rotate dh without rotating identity? If x-sk derives from same seed, no independence — fine, just say card carries `dh: <x25519 pk>`; implementations derive it deterministically from seed (so no second secret to store), but senders just read it from the card. That's minimal and honest. And it dodges the cross-protocol key-reuse debate entirely (different key material for DH vs signing — derived from same seed but functionally separated; flag: seed compromise = both, but seed never leaves device anyway). Good — simpler spec, no birational formulas. Senders MUST verify dh key came from a valid card (they did — card verified).

Now the AEAD: keys: ck random 32B per item. k1=SHA256("fp1/enc"||ck), k2=SHA256("fp1/mac"||ck). Stream: block_i = SHA256(k1||nonce||uint64_be(i)); ct = pt XOR stream. Tag = HMAC-SHA256(k2, "fp1/aead"||nonce||ct). Encrypt-then-MAC; verify before decrypt. Wraps: for each recipient r with dh pk R: shared = X25519(e_sk, R); wk = SHA256("fp1/wrap"||shared||e_pk); wrap = ck XOR wk. Recipient: for each wrap: candidate ck = wrap XOR SHA256("fp1/wrap"||X25519(d_sk,e_pk)||e_pk); then k2' derive, verify tag; if pass → decrypt. Cost per item: 1 DH + (per wrap) 1 SHA + 1 HMAC... verifying tag requires full ct HMAC per wrap — O(wraps × ct size) hashing worst case; optimize: try wraps against a *check value* instead: include in item a `ck
```

</details>

