{
  "name": "open-feed",
  "version": "0.0.0",
  "private": true,
  "description": "The Open Feed Protocol: the specification, its zero-dependency reference implementation, and the examples that teach it",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": "./src/openfeed.js"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "vectors": "node tools/regen.js",
    "examples": "node tools/examples.js",
    "revert": "node tools/revert.js",
    "seeds": "sh -c 'for f in examples/_seeds/*-gate.js; do node \"$f\" >/dev/null || { echo \"RED: $f\"; exit 1; }; done; echo \"all seeds green\"'",
    "check": "npm test && npm run vectors && npm run examples && npm run seeds"
  }
}
