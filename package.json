{
  "name": "open-feed",
  "version": "0.0.0",
  "private": true,
  "description": "The Open Feed Protocol: the specification, its zero-dependency reference implementation, and the examples that teach it",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": "./src/openfeed.js"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "spec": "node tools/spec.js",
    "vectors": "node tools/regen.js",
    "tldr": "node tools/tldr.js",
    "check": "npm test && npm run spec && npm run vectors && npm run tldr"
  }
}
