{
  "name": "open-feed",
  "version": "0.1.0",
  "private": true,
  "description": "The Open Feed Protocol: the specification, its zero-dependency reference implementation, and the examples that teach it",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": "./src/openfeed.js"
  },
  "bin": {
    "openfeed": "bin/openfeed.js"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "spec": "node tools/spec.js",
    "vectors": "node tools/regen.js",
    "tldr": "node tools/tldr.js",
    "check": "npm test && npm run spec && npm run vectors && npm run refs && npm run tldr",
    "refs": "node tools/refs.js"
  }
}
