---
model: kimi-k3
pass: 2
endpoint: https://opencode.ai/zen/go/v1/chat/completions
date: 2026-08-20T22:04:20.822Z
prompt: PROMPT-pass2.md
prompt_sha256: e808057730fa1e8528c594da5513c840a16897c944e6bf2b03a576244d5ed8f5
reasoning_effort: medium
finish_reason: null
tokens: null
wall_seconds: 137
truncated: false
reasoning_chars: 16907
---


<details><summary>Model reasoning trace (16907 chars) — kept because rejected alternatives often live here</summary>

```
Let me work through this carefully. The task has three parts: attack the other team's design, attack my own, reconcile, then answer the five open questions.

First, let me understand the other team's design deeply:

**Other team's design ("pull-everything")**:
- Identity = key, signed profile names locations, keys, recovery arrangement
- Relocation: signed location list, readers remember all locations, check others when stale; strangers lost
- Device-only signer, hub = storage; bring-your-own-client as security property; publish interface spec (signed PUT at conventional paths)
- Signed items are files, signed as bytes; feeds are generated views
- One attestation shape: "B succeeds A" signed by A or by enough trusted peers
- Publisher forgets, readers remember; tombstones
- Completeness: signed sequenced head listing what exists; reader pins it, notices vanish-without-tombstone. No freshness deadline.
- Everything pull: replies/reactions are items in the replier's own feed naming target. No inbox. Push ping / inbound interop / DMs are extensions.
- Three tiers: public; encrypted to set of keys, recipients sealed; DM = one recipient. Hub learns existence/timing/size, not whom. Clients poll on fixed cadence.

Open questions: publish interface shape; encryption construction (in-house, unreviewed — wants audited off-the-shelf, but brief bans third-party libraries!); social recovery threshold & trust set publication; what is "the head"; whether push should exist.

**Part A: Attack the other design.**

The biggest failure: **everything is pull**. Let me think end-to-end about the divorce scenario.

In the divorce scenario, the woman's private replies... wait, actually, the key attack: with everything pull, a private reply lives in the replier's feed on the replier's hub. For the original author to see replies to her family-only post, she must poll the feeds of everyone who might reply. How does she know who to poll? Social knowledge — her contacts. OK, fine at family scale. But the real problem is metadata + the hostile hub:

Attack 1: **The pull model hands the hostile hub the social graph and the read graph.** In my design, her ex's hub never sees delivery edges. In theirs, every reader must fetch her feed from his hub. His hub logs every fetch: who (IP) reads her, when. He gets the complete reader set for public content. For encrypted content, they say "clients poll on a fixed cadence so a fetch proves nothing" — but fixed cadence polling only works if *all* clients poll *all* feeds at the same cadence, which is a global coordination requirement that's unenforceable and expensive; in practice a fetch spike after a post is a signal. And more importantly: the hub is the serving path, so the hostile operator sees exactly which IPs pull which encrypted items. He can't read content but he can map her social circle. Hmm, but my design also leaks recipient hub → recipient delivery edges... actually no: in my design her device connects to recipients' hubs, and the recipient's hub (if hostile, e.g., the same ex's hub!) sees inbound envelopes. If mom is also on his hub, he sees envelopes arriving for mom. But the author addressing is hidden. In theirs, pull means the reader reveals themselves to the author's hub.

But the brief says name the failure they didn't see, one attack spelled end to end. Let me think about the strongest one.

**The killer attack: everything-pull + "publisher forgets" + hostile hub = the reply suppression / isolation attack, or worse: the hostile hub as the only rendezvous means he can sever her from the family conversation without her knowing.**

Actually, let me think about what's actually fatal. Consider the divorce. Her content is on his hub. Family-only thread: she posts (encrypted, on his hub). Replies live in the repliers' own feeds on their hubs (mom's hub, sister's hub). To read the thread, her app polls mom's hub and sister's hub. Fine. But wait — how do mom and sister discover her new post? They poll her feed on his hub. He can serve them stale views selectively — split views per-reader. In my design, split-view exists too but private delivery pushes envelopes to recipient hubs, so recipients learn of new content from their own hub even if her hub freezes. In their design, there is no push; mom's app polls her feed on his hub at a fixed cadence; he simply serves mom a frozen feed. The signed sequenced head lets mom "notice when something she saw vanishes" — but not notice what she never saw. Completeness detection is retrospective: you can only pin what you've been served. A hub that serves a stale head to a specific reader is undetectable by that reader without an independent channel. My design has the same problem for the author's hub serving stale public content, but private content delivery bypasses the author's hub entirely — the envelope lands on the recipient's own hub. So the withholding window in mine is limited to public posts; in theirs it covers everything, including the family-only conversation that is the heart of the divorce scenario.

And worse: he can do this *selectively* — serve the daughter fresh content but freeze the sister, for weeks. Since cadence polling is fixed and fetch proves nothing, the sister's app has no alarm. The design says "no freshness deadline... deliberately." That's the gap: they chose one-paragraph completeness over freshness, and in the exact scenario the brief centers, freshness-withholding is the hostile operator's strongest remaining weapon after confidentiality and authenticity are cryptographically closed.

Sequence, concrete:
1. Divorce. She stays on his hub (hasn't left yet — leaving takes days to arrange).
2. She posts family-only updates; his hub serves them to... he chooses. He serves fresh content to the kids (maintaining appearance) and a frozen head to her mother and sister.
3. Mom and sister's apps show her feed "current" — head verifies, signature valid, sequence monotonic — just old. There is no tombstone absence to detect because they never saw the items.
4. Replies: mom replies in her own feed on her own hub; mom's feed items are encrypted to the family set. He polls... wait, does he poll mom's feed? He can — location discovery is via profile. He fetches mom's feed, sees encrypted items exist (timing, size). He can't read them. But he can infer: mom posted something encrypted 20 minutes after her daughter's post time... but he froze what he serves mom, so mom is replying to old posts. The family conversation quietly desynchronizes: kids see current, mom sees two weeks stale, replies reference items half the family can't see. The thread content-hash references would reveal existence! If mom's reply names `root: h:…` of an item mom hasn't seen... wait, mom replies to what mom has seen; the kids see mom's reply referencing an old root while they've seen newer posts. Does any reader get an alarm? The kids' apps see mom's replies referencing an old position — but "replies to an old post" is normal human behavior; no verdict lattice exists to flag "mom is being served stale." They explicitly refused the stale verdict lattice. So the design cannot distinguish "mom is behind" from "mom is being starved." 

5. When she finally leaves (move statement, higher seq, new location), his hub serves... the move statement is in her profile, which readers poll — he serves the old profile to mom. "A location statement with a higher sequence wins" only if you see it. Readers "remember every location and check the others when the primary goes stale" — but the primary isn't stale, it's frozen-but-plausible: he's still serving content, signed head seq 212, and he just never serves seq 213+. Her new posts go to the new hub; his hub's view of her stops advancing. Is that "stale"? Their design has no freshness deadline, so mom's app has no rule that says "2 weeks without a post from a daily poster = check alternates." And the alternates mom remembers are all his prior locations — same operator. The new location statement lives at the new hub; mom's app has no reason to fetch there because it doesn't know it exists. How does mom learn the new location in the pull-only model? She polls the old profile, he freezes it. She's severed. The design says "Strangers who only knew the old location may simply be lost" — but mom is not a stranger, and she's lost too, because the only rendezvous was the hostile hub and there is no push channel. In my design the move notice is pushed by her device to mom's hub inbox — his hub is not on the path. That's the crux: **their relocation mechanism routes through the adversary**.

So the attack, end to end: the hostile operator exploits (a) pull-only discovery, (b) no freshness deadline, (c) reader-side completeness-only detection, to partition the family and then to permanently sever her relocation — all while serving only validly signed objects. Every check the design specifies passes. Scenario 1 breaks at "her mother's app follows her with one tap": there is nothing to tap, because nothing arrived.

Also scenario 4 (domain goes): "readers remember every location and check the others when the primary goes stale" — when the domain dies, fetches fail (connection refused), that IS detectable as stale, apps check alternates. OK that works for death, but not for freeze. Distinguish: unavailability is detectable; selective withholding is not. Good nuance.

Scenario 3 (two hubs one thread): pull-based threading requires A to poll B's hub and vice versa. That's fine. But the family-only thread: B's reply is encrypted to the family set, published on family.example; A polls family.example for B's feed. Family.example learns A's IP fetches B's feed — access-control free, fine. It works, actually, modulo metadata. The metadata claim "hub learns nothing about whom" is only about the item headers; the *fetch graph* leaks the reader graph. And fixed-cadence polling to mask fetches: at family scale, polling 30 relatives' feeds every 5 minutes forever is the cost; and the moment an app fetches-on-notification (extension push ping), the masking collapses.

Also the "bring-your-own-client is the security property" + "publish interface" — I actually think this is *good*, and I should say so in the "what's right" section. My design didn't have a publish interface — my apps upload to hubs but I never specified the write API as a first-class interop surface! That's a real omission in my design: "hub API has no endpoint accepting key material" but I never specified the upload protocol, so client-hub lock-in creeps back in. Their insight — that the security property is client independence, since a hub shipping the app can take the key — is sharp and correct. In the divorce scenario, if he operates the hub AND the only app she can use with it, everything else is moot. My design assumed "her app holds the key" without defending the app supply chain.

What's right in their design:
1. Bring-your-own-client + publish interface as a specified surface. Better than mine.
2. Signed files at conventional paths, feeds as generated views — simpler than my envelope-wrapping? Actually similar. Their "signed as the bytes served" = my sign-the-body-bytes. Same.
3. One attestation shape for rotation and social recovery — arguably more elegant than my rotate-vs-vouch split. I had two mechanisms (rotation chain in card + vouch entries); they unify: "B succeeds A" valid if signed by A OR threshold of trusted peers. Unification is genuinely elegant. But: it has a hole — who are "peers a reader already trusts"? Trust set never published (good for privacy), so the reader must have local trust state; a new reader (stranger, scenario 7) has no trust set and must rely on A's signature only — fine, that's consistent with non-goals. The problem: threshold recovery lets a quorum of peers *rotate a key without the subject*. Collusion of threshold-many family members can seize her identity — and in a hostile family, that's not paranoid: the ex campaigns among the in-laws. Threshold social recovery turns family politics into a key-compromise vector. My vouch design kept new-inception + vouch, so the old identity is never "taken over" — the new key is a new identity that happens to be believed to be the same person. Theirs preserves *identifier continuity* under social recovery, which is stronger UX but hands a quorum the crown. Also "B succeeds A signed by A" — thief with A's key rotates; same as mine. Equivalent there.
4. "Publisher forgets; readers remember" + tombstones — this is a genuinely better default than my implicit permanence. My design never addressed deletion at all! I never specified delete/revoke. Their tombstone story is right: you cannot claw back what readers fetched, and pretending otherwise is dishonest; a signed tombstone requests removal and readers' compliance is voluntary. Honest.
5. No freshness deadline — I attack this above, but the *honesty* of stating the completeness story in one paragraph is good. My hash chain gives completeness of the past too but I never gave readers a signed head! Actually my log has entries chained; the latest entry is a de facto head. Hmm, my readers verify prev-links; a withheld suffix is undetectable in mine too except via head gossip. Their pinned-head is a cleaner statement of the same detection. Equivalent-ish, but theirs is explicit.

More attacks on theirs (pick the top ones, spell out #1 fully, list others):

Attack 2: **Encryption construction "written in-house, never reviewed" + the brief bans third-party libraries.** They want to swap for "an audited off-the-shelf construction" — but the brief's priority #1 forbids taking on a library. Audited constructions you can *implement from a standard library* are few: NaCl box is a library; age is a spec+library. The honest reading: you may *copy a construction* (X25519-HKDF-AEAD, as I did) but you can't depend on libsodium. Their open question #2 reveals they may be about to either violate constraint #1 or ship homebrew crypto. The failure mode: scenario 6, weekend implementer implements "the construction" from the text — if the text says "use an audited library," the implementer on a stdlib-only target is stuck; if the text specifies homebrew, assurance 2 rests on unreviewed crypto. Either way a floor item wobbles. This is a contradiction they've noticed but not resolved.

Attack 3: **Social recovery threshold as key-succession = hostile-family quorum attack** (above). Scenario: he holds... let's construct: threshold 3-of-trusted-peers. Divorce. He is (was) in her family's trusted sets? "Peers a reader already trusts" — the trust is per-reader: mom trusts {her other children}. For the attacker to forge a succession that mom accepts, he needs threshold-many of *mom's* trusted peers to sign. He can't sign as them (they hold keys). But he can *socially engineer*: he is the kids' father; he gets two of her trusted contacts' devices/signatures ("mom's locked out, sign this to help her recover") — classic recovery-collective attack. The design hands a motivated insider a formal procedure: collect threshold signatures, publish "B succeeds A," and readers auto-accept (do they? if acceptance is automatic, it's catastrophic; if it requires a tap, then it's my vouch UX with extra steps). The spec's silence on whether succession is auto-accepted is the open flank. In my design, vouch acceptance is explicitly one-tap human-confirmed with an instruction to call. Theirs must answer the same or the quorum attack is scriptable. Also: since trust sets are never published, the attacker doesn't know *whose* signatures a given reader requires — that helps a bit (defender's advantage), but at family scale the trust set is guessable (the family).

Attack 4: fixed-cadence polling economics — 10k users on the big lazy hub, each polling every contact every N minutes. Scenario 5 says hub per-identity cost stays flat — polling load is O(contacts × rate), fine, flat-ish per identity. OK, not fatal, but "clients poll on a fixed cadence so a fetch proves nothing" is undermined by the push-ping extension the moment it's adopted: once pings exist, fetch timing re-correlates. The metadata protection is fragile against the system's own extension path. And the hub can also *inject* timing signals: serve reader X a new item, watch which other fetches follow — active traffic analysis by the serving hub. With pull, the hub is positioned to run active probes (selectively serve item to one reader, observe who else fetches soon after → reader graph). My push model doesn't let the author's hub do this because delivery edges don't traverse it. Hmm, but recipient's hub sees envelope arrival → recipient app polls own hub → timing. The recipient's hub is the one that could probe, but it can't select content. The author's-hub probing attack is unique to pull. This is a good secondary attack:
```

</details>

