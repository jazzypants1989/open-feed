§3.7 — where she is hosted changed; who she is did not

  before   version 1   locations ["https://pence.family/alice"]
  after    version 2   locations ["https://pence.family/alice","https://alice.example/alice"]
  anchor   pukq6VMQM9Sbp4ae71bJjcKbLLpFuWi47cDS60xH7CY   — the same key signed both files, and it is the identity
  the link she sends her people: https://alice.example/alice#pukq6VMQM9Sbp4ae71bJjcKbLLpFuWi47cDS60xH7CY  (§3.1, examples/first-contact/)

§3.7 — a reader remembers every location a verified profile has ever named

  read at pence.family   the profile names pence.family                 remembered: pence.family
  read at alice.example  the profile names pence.family, alice.example  remembered: pence.family, alice.example
  read at alice.example  the profile names alice.example                remembered: pence.family, alice.example

  Version 3 dropped pence.family. The reader did not: the remembered set only grows.

§13.3 — the reader who never learns the new location is the honest limit

  sis, who only ever knew pence.family:  ok — version 1, top 1, notes: none
    the posts she sees: 1  — post 2 does not exist for her, and nothing on the page says so
  mum, whose pin followed alice:         identity — an older profile than the one this reader saw

  Not an error and not a redirect: an unmarked page. Only a reader that has been
  somewhere else can read the frozen copy as old.

§3.7 — when one location stops answering, the reader tries the others

  pence.family    no verdict — the name does not resolve (§9)
  alice.example   ok — version 3, top 2

  She found alice again without asking anybody, and without the old host doing a thing.

§5.4 — the address rides along in other people's posts

  mum's reply, target:  {"key":"pukq6VMQM9Sbp4ae71bJjcKbLLpFuWi47cDS60xH7CY","n":2,"hash":"Tu0BH3mAvyuHwggsVs2wRygxeI6DBAk0v3Ki3znWx4c","loc":"https://alice.example/alice"}
  sis held for alice:   version 1, top 1, locations [pence.family]
  what her reader fetched, in order:
    pence.family/alice/profile
    alice.example/alice/profile
    alice.example/alice/index
    alice.example/alice/posts/1
    alice.example/alice/posts/2
  sis holds for alice:  version 3, top 2, locations [pence.family, alice.example]
  rumors raised:        none — she can see the post now

§5.4 — a `loc` aims a fetch, and only what verifies there moves anybody

  bro's own post, which verifies as his. What her reader fetched, in order:
    pence.family/alice/profile    the locations she already holds are tried first (§7.5)
    alice.example/alice/profile
    alice.example/alice/index
    alice.example/alice/posts/1
    alice.example/alice/posts/2
    bro.example/alice/profile     the address in his reply, tried last
    what bro.example served for her: identity — the profile is not signed by the key it ends on
    sis still holds for alice:       version 3, top 2, locations [pence.family, alice.example]
    rumors raised: bro replied to something I cannot see  — the replier is the only party there is evidence about

  What is served at a `loc` must verify under the anchor key the reader learned (§3.1, §7.1),
  or the pin does not move. An encrypted reply carries its target inside the envelope (§6.5,
  examples/envelope/), so relocation rides along in public replies only.

Every line above is asserted.
